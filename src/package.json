{
  "name": "email-to-notion",
  "version": "1.0.0",
  "description": "Forward emails to a Notion database",
  "main": "index.js",
  "scripts": {
    "test": "node test.js",
    "build": "npm ci --omit=dev"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.71.2",
    "@aws-sdk/client-ssm": "^3.0.0",
    "@notionhq/client": "^5.4.0",
    "postmark": "^4.0.5",
    "turndown": "^7.2.2",
    "turndown-plugin-gfm": "^1.0.2"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
